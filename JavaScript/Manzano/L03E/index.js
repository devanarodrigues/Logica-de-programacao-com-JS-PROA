let cont = 0
let contNumero = 0
let mult = 0


while (cont <= 15) {
    while (contNumero <= cont) {
        mult == 0 ? mult = 1 : mult = mult * 3
        console.log(`Exponente ${cont}: ${mult}`)
        contNumero++
    }
    cont++
}

